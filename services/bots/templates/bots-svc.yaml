apiVersion: v1
kind: Service
metadata:
  name: bots-svc
  namespace: apps
spec:
  ports:
    - protocol: TCP
      port: {{ .Values.env.GRPC_PORT }}
      targetPort: grpc_port
  selector:
    app: bots-app
  type: ClusterIP
