apiVersion: v1
kind: Service
metadata:
  name: replay-svc
  namespace: apps
spec:
  ports:
    - protocol: TCP
      port: {{ .Values.env.REPLAY_PORT }}
      targetPort: {{ .Values.env.REPLAY_PORT }}
  selector:
    app: replay-app
  type: ClusterIP
